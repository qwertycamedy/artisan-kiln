import { Panel } from "@/components/ui";
import { CartTable } from "@/components/shared";

export const CartPanel = () => {
  return (
    <Panel title="Shopping Cart & Design Tool">
      <CartTable />
    </Panel>
  );
};
